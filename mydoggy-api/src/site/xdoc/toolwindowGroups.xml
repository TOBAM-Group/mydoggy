<!DOCTYPE document PUBLIC
        "-//Apache Software Foundation//DTD XDOC 1.0//EN"
        "http://maven.apache.org/dtd/xdoc_1_0.dtd">
<document>
    <properties>
        <title>MyDoggy-Api : ToolWindowGroup</title>
        <author email="angelo.decaro@gmail.com">Angelo De Caro</author>
    </properties>

    <body>
        <section name="Groups - ToolWindowGroup">
            <p>
                The idea is to provide the same mechanism of prospectives of eclipse ide for tools window.
                The typical usage is to retrieve a group from the tool window manager and to add any tools window.
                After that we can show or hide all tools registerd in a group.                
            </p>

            <table>
                <tbody>
                    <tr>
                        <td>
                            <img src="./images/toolWindowGroup_1.png" alt="ToolWindowGroup" border="0" hspace="18"/>
                        </td>
                        <td>
                            <p>
                                To obtain an instance of <code>ToolWindowGroup</code> interface use the following:
                            </p>
                            <source>ToolWindowManager toolWindowManager = new <a href="./../mydoggy-plaf/index.html">...</a>;
ToolWindowGroup group = toolWindowManager
             .getToolWindowGroup(
                 "main"  // ToolWindowGroup name
              );</source>
                            <p>
                                If the manager contains no mapping for this name then the manager create a new instance
                                of ToolWindowGroup and associate the group created with the specified name in the manager.
                                <br></br>
                                After that, you can populate the group.
                            </p>
                            <source>mainGroup.addToolWindow(
             toolWindowManager.getToolWindow(0)
          );</source>
                            <p>
                                To shor or hide a group use the following:
                            </p>
                            <source>mainGroup.setVisible(
             true   // true to show, false to hide
          );</source>
                        </td>
                    </tr>
                </tbody>
            </table>
        </section>
    </body>
</document>
