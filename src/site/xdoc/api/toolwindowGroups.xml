<!DOCTYPE document PUBLIC
        "-//Apache Software Foundation//DTD XDOC 1.0//EN"
        "http://maven.apache.org/dtd/xdoc_1_0.dtd">
<document>
    <properties>
        <title>MyDoggy - My Java Docking Framework (API) : ToolWindowGroup</title>
        <author email="adecaro@users.sourceforge.net">Angelo De Caro</author>
    </properties>

    <body>
        <section name="ToolWindowGroup">
            <p>
                The idea is to provide the same mechanism of the prospectives from eclipse ide for toolwindows.
                The typical usage is to retrieve a group from the tool window manager and to add any toolwindows
                one wants.
                After that we can show or hide all tools registered in that group.
            </p>

            <table>
                <tbody>
                    <tr>
                        <td>
                            <img src="../images/api/toolWindowGroup_1.png" alt="ToolWindowGroup" border="0" hspace="18"/>
                        </td>
                        <td>
                            <p>
                                To obtain an instance of <code>ToolWindowGroup</code> interface use the following:
                            </p>
                            <source>ToolWindowManager toolWindowManager = new <a href="./../mydoggy-plaf/index.html">...</a>;
ToolWindowGroup group = toolWindowManager
             .getToolWindowGroup(
                 "main"  // ToolWindowGroup name
              );</source>
                            <p>
                                If the manager contains no mapping for name passed to the
                                <code>getToolWindowGroup</code>
                                method then
                                the manager create a new instance of ToolWindowGroup and associate the group created
                                with the specified name in the manager.
                                <br/>
                                After that, you can populate the group.
                            </p>
                            <source>mainGroup.addToolWindow(
             toolWindowManager.getToolWindow(0)
          );</source>
                            <p>
                                To show or hide a group use the following:
                            </p>
                            <source>mainGroup.setVisible(
             true   // true to show, false to hide
          );</source>
                        </td>
                    </tr>
                </tbody>
            </table>
        </section>

        <section name="Using ToolWindowGroup">
            <subsection name="Get a group">
                Use the following :
                <source>
ToolWindowGroup group = toolWindowManager.getToolWindowGroup("group_name");
</source>
                <p>
                    Returns the tool window group to which this manager maps the specified name.
                    If the manager contains no mapping for this name then the manager create a new instance
                    of ToolWindowGroup and associates the group created with the specified name in this manager.
                </p>
            </subsection>
            <subsection name="Remove a group">
                Use the following :
                <source>
toolWindowManager.removeToolWindowGroup("group_name");
</source>
                <p>
                    Removes the tool window group for this name from this manager if it is present.
                </p>
            </subsection>
            <subsection name="Check if a group exists">
                Use the following :
                <source>
boolean contains = toolWindowManager.containsGroup("group_name");
</source>
                <p>
                    Returns <tt>true</tt> if this manager contains a group for the specified name.
                </p>
            </subsection>
            <subsection name="Add a toolwindow">
                Use the following :
                <source>
ToolWindowGroup group = toolWindowManager.getToolWindowGroup("group_name");
ToolWindow debugTool = toolWindowManager.getToolWindow("Debug");
group.addToolWindow(debugTool);
</source>
                <p>
                    Appends the specified tool window to the end of this group.
                </p>
            </subsection>
            <subsection name="Remove a toolwindow">
                Use the following :
                <source>
ToolWindowGroup group = toolWindowManager.getToolWindowGroup("group_name");
ToolWindow debugTool = toolWindowManager.getToolWindow("Debug");
group.removeToolWindow(debugTool);
</source>
                <p>
                    Removes the occurrence in this group of the specified tool window.
                </p>
            </subsection>
            <subsection name="Show or hide a group">
                Use the following :
                <source>
ToolWindowGroup group = toolWindowManager.getToolWindowGroup("group_name");
group.setVisible(true);
</source>
                <p>
                    Invokes <code>setVisible(visible)</code> on all toolwindows contained in this group
                    in proper sequence.
                </p>
            </subsection>
            <subsection name="Add a ToolWindowGroupListener">
                Use the following :
                <source>
ToolWindowGroup group = toolWindowManager.getToolWindowGroup("group_name");
group.addToolWindowGroupListener(listener);
</source>
                <p>
                    Registers <code>listener</code> so that it will receive events when
                    the groups are made visibile or invisible, when a tool is added or removed.
                    If listener <code>listener</code> is <code>null</code>,
                    no exception is thrown and no action is performed.
                </p>
            </subsection>
        </section>
    </body>
</document>
