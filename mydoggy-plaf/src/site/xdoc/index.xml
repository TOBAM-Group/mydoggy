<!DOCTYPE document PUBLIC
        "-//Apache Software Foundation//DTD XDOC 1.0//EN"
        "http://maven.apache.org/dtd/xdoc_1_0.dtd">
<document>
    <properties>
        <title>MyDoggy-Plaf : Introduction</title>
        <author email="angelo.decaro@gmail.com">Angelo De Caro</author>
    </properties>

    <body>
        <section name="Introduction">
            <p>
                To obtain an instance of <code>ToolWindowManager</code> interface you have to instantiate the
                <code>MyDoggyToolWindowManager</code> class. This is the default implementation.
                <br></br>
                To obtain an instance use the following (for example):
            </p>
            <source>ToolWindowManager toolWindowManager = new MyDoggyToolWindowManager(
              frame,      // A window implementing the RootPaneContainer interface
              Locale.getDefault() // A Locale instance
);
            </source>
            <p>
                A window implementing the <code>RootPaneContainer</code> interface is required becouse the default
                implementation use the glass pane for the <code>ToolWindowType.SLIDING</code> type.
            </p>
        </section>
    </body>
</document>
